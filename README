# Outline

- Basic concepts
  - Linking and namespaces, `nm`
  - Name mangling and `extern "C"`

- Basic FFI
  - Shared libraries, manual lookup
    - `ctypes`

- Language-specific tools (`Python.h`, `pybind11`, etc.)

- Common concerns
  - Error handling
  - Cross-language memory management

- General frameworks (SWIG)
  - Powerful, but usually lower quality than something hand-written
